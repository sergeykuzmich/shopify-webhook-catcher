name: QA

on: workflow_dispatch

jobs:
  build:
    name: QA
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v2
      - name: Run QA
        run: |
          make qa
